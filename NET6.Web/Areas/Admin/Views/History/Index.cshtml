@using NET6.Web.Consts
@model List<HistoryDto>

<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Öz Geçmiş Yönetimi /</span> Öz Geçmişler
    </h4>

    <!-- Basic Bootstrap Table -->
    <div class="card">
        <div class="table-responsive text-nowrap m-5">

            @{
                @if (!User.IsInRole($"{RoleConsts.User}"))
                {
                    <a class="btn btn-success mb-3" asp-area="Admin" asp-controller="History" asp-action="Add">
                        Öz Geçmiş Ekle <i class="bx bx-plus"></i>
                    </a>
                }
            }



            <table class="table table-bordered" id="historiesTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>Öz Geçmiş Adı</th>
                        <th>Kategori</th>
                        <th>Oluşturulma Tarihi</th>
                        <th>Oluşturan</th>
                        <th>Durum</th>
                        @{
                            @if (!User.IsInRole($"{RoleConsts.User}"))
                            {
                                <th>İşlemler</th>
                            }
                        }
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>Öz Geçmiş Adı</th>
                        <th>Kategori</th>
                        <th>Oluşturulma Tarihi</th>
                        <th>Oluşturan</th>
                        <th>Durum</th>
                        @{
                            @if (!User.IsInRole($"{RoleConsts.User}"))
                            {
                                <th>İşlemler</th>
                            }
                        }

                    </tr>
                </tfoot>
                <tbody class="table-border-bottom-0">
                    @foreach (var history in Model)
                    {
                        <tr name="@history.Id">
                            <td>@history.Title</td>
                            <td>@history.Category.Name</td>
                            <td>@history.CreatedDate.ToShortDateString()</td>
                            <td>@history.CreatedBy</td>
                            <td>@(history.IsDeleted ? "Aktif Değil" : "Aktif")</td>
                            @{
                                @if (!User.IsInRole($"{RoleConsts.User}"))
                                {

                                    <td>

                                        <a class="btn btn-primary btn-sm" data-id="@history.Id" asp-area="Admin" asp-controller="History" asp-action="Update" asp-route-articleId="@history.Id"><i class="bx bxs-edit"></i></a>
                                        <a class="btn btn-danger btn-sm" data-id="@history.Id" asp-area="Admin" asp-controller="History" asp-action="Delete" asp-route-articleId="@history.Id"><i class="bx bx-minus-circle"></i></a>


                                    </td>
                                }
                            }
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <!--/ Basic Bootstrap Table -->




</div>

@section Scripts{
    <script src="~/js/historyIndex.js" type="application/ecmascript"></script>
}