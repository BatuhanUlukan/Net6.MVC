@model List<HistoryDto>

<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Çöp Kutusu /</span> Öz Geçmiş
    </h4>

    <!-- Basic Bootstrap Table -->
    <div class="card">
        <div class="table-responsive text-nowrap m-5">

            <table class="table table-bordered" id="T" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>Öz Geçmiş Adı</th>
                        <th>Kategori</th>
                        <th>Oluşturulma Tarihi</th>
                        <th>Oluşturan</th>
                        <th>Durum</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>Öz Geçmiş Adı</th>
                        <th>Kategori</th>
                        <th>Oluşturulma Tarihi</th>
                        <th>Oluşturan</th>
                        <th>Durum</th>
                        <th>İşlemler</th>
                    </tr>
                </tfoot>
                <tbody class="table-border-bottom-0">
                    @foreach (var history in Model)
                    {
                        <tr name="@history.Id">
                            <td>@history.Title</td>
                            <td>@history.Category.Name</td>
                            <td>@history.CreatedDate.ToShortDateString()</td>
                            <td>@history.CreatedBy</td>
                            <td>@(history.IsDeleted ? "Aktif Değil" : "Aktif")</td>
                            <td>
                                <a class="btn btn-warning btn-sm" data-id="@history.Id" asp-area="Admin" asp-controller="History" asp-action="UndoDelete" asp-route-articleId="@history.Id"><i class='bx bx-undo'></i></a>
                                @*<a class="btn btn-danger btn-sm" data-id="@article.Id" asp-area="Admin" asp-controller="Article" asp-action="Delete" asp-route-articleId="@article.Id"><i class="bx bx-minus-circle"></i></a>*@


                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <!--/ Basic Bootstrap Table -->




</div>

@section Scripts{
    <script src="~/js/tIndex.js" type="application/ecmascript"></script>
}